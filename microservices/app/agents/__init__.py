"""Agent stubs for the Classroom Director, Lesson Planner, and Student Personas.

These return deterministic scaffold responses so the full Create → Simulate → Teach
loop works without LLM keys. Swap implementations when wiring real models.
"""
